name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.2.8
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.23.1
  esc_pos_utils: ^0.3.6
  # esc_pos_utils:
    # path: ../esc_pos_utils
  flutter_bluetooth_basic: 
    git: https://github.com/wisnuwiry/flutter_bluetooth_basic.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
